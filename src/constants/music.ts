/**
 * Song Arranger - Music Constants
 * Time signature, tempo, and clip duration settings
 */

/**
 * Number of beats per bar (4/4 time signature)
 */
export const BEATS_PER_BAR = 4;

/**
 * Default tempo in BPM
 */
export const DEFAULT_TEMPO = 120;

/**
 * Minimum allowed tempo in BPM
 */
export const MIN_TEMPO = 20;

/**
 * Maximum allowed tempo in BPM
 */
export const MAX_TEMPO = 300;

/**
 * Minimum clip duration in beats
 */
export const MIN_CLIP_DURATION = 1;

/**
 * Default clip duration in beats (1 bar)
 */
export const DEFAULT_CLIP_DURATION = 4;
